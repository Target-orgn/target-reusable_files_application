name: Use Shared Workflow
'on':
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to deploy to
        required: true
        default: production
jobs:
  call-shared:
    uses: Target-orgn/reusable_files-post/.github/workflows/build_file.yml@demo1
    with:
      environment: ${{ inputs.environment }}
